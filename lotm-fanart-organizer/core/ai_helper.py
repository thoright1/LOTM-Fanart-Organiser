from __future__ import annotations

from pathlib import Path


def ai_predict(image: Path) -> tuple[str, float] | None:
    """Placeholder AI hook. Deterministic rules always take precedence."""
    return None
